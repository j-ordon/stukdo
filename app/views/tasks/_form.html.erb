<%= simple_form_for(@task) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, label: false, placeholder: "Enter your task" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
